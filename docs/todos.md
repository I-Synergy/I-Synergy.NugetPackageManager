- the installed tab checks for all projects in the folder instead of projects in the solution (like update does)
- the Directory.Packages.config/cs project files use conditions like "Condition="$(TargetFramework.StartsWith('net8.0'))" to specify packages for multiple framework versions. Updates are not handled correctly causing them to be placed in the general packageversion itemgroup. I want to be able to select a certain framework as see which updates are available. So updated need to be grouped by framework and update them accordingly. This must be very clear in the UI. If we don't use multiple framework versions, the ui should stay as is.